#include"stdafx.h"
#include<iostream>
#include"ausdruck.h"



/*int main() {
	while (true)
	{
		std::cout << "Bitte geben Sie einen auszuwertenden mathematischen Ausdruck ein(e um die Eingabe zu beenden, nach jeder Zahl/Operation Enter)!";
		char ch;
		do {
			std::cout << "\n>>";
			std::cin.get(ch);
			if (ch != 'e') {
				std::cout << ausdruck(ch);
			}
		} while (ch != 'e');

	}
}
*/